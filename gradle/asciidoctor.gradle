asciidoctorj {
    // We can separately change the AsciidoctorJ dependency version.
    version = '1.5.4.1'
}

asciidoctor {
    sourceDir = file('doc')
    outputDir = file("$buildDir/doc")
    separateOutputDirs = false

    requires  'asciidoctor-diagram'
}
